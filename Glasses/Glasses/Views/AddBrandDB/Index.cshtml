@model IEnumerable<Glasses.Models.Brand>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add Brand</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Glassbrand)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Glassbrand)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Brand_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Brand_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Brand_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
